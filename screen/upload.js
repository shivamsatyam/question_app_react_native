{
  /*
  
  	const uploadTask = storage.ref('/images/'+imgName).put(blob)
	uploadTask.on(
			"state_changed",(snapshot)=>{
				//progress bar function
				console.log('the snapshot is ',snapshot)	
			
			},
			(error)=>{
				//Error function.....
				console.log('the upload error is ',error)
				alert(error.message)
			},
			()=>{
				//complete function
				storage.ref("images")
				.child(image.name)
				.getDownloadURL() 
				.then((url)=>{
          console.log("the downloaded uri is  ")

          console.log(url)
				})
			}
			)




      
          <View
            style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Button
              title="Pick an image from camera roll"
              onPress={pickImage}
            />
            {image && (
              <Image
                source={{ uri: image }}
                style={{ width: 200, height: 200 }}
              />
            )}
          </View>
  */ 
























  /*
  
      <View style={styles.test_options_view}>
        <RadioButtonRN
          data={currentQuestion}
          selectedBtn={(e) => console.log(e)}
          icon={<Icon name="check-circle" size={25} color="#2c9dd1" />}
        />
      </View>

      <View style={styles.test_button_view}>
          <Button title="Previous" style={styles.test_button}/>
          <Button  title="Next" style={styles.test_button}/>
      </View>
    </View>
  
  
  */ 
}

